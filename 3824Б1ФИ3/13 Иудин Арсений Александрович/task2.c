// Задача 2. Дан непустой массив A, содержащий n натуральных чисел.
// Назовем «серией» группу подряд идущих одинаковых элементов, 
// а длиной серии – количество этих элементов (длина серии может быть равна 1).
// Найти длину наибольшей серии.
#include <stddef.h> 
int task2(unsigned A[], size_t n) {
    if (n == 0) {
        return 0; // Если массив пустой, возвращаем 0 (по условию задача не предполагает пустой массив)
    }

    unsigned max_length = 1; // Максимальная длина серии, начинаем с 1
    unsigned current_length = 1; // Текущая длина серии

    for (size_t i = 1; i < n; i++) {
        if (A[i] == A[i - 1]) {
            current_length++; // Увеличиваем текущую длину, если элемент равен предыдущему
        } else {
            if (current_length > max_length) {
                max_length = current_length; // Обновляем максимальную длину, если текущая больше
            }
            current_length = 1; // Сбрасываем текущую длину
        }
    }

    // Проверяем в конце, если последняя серия была максимальной
    if (current_length > max_length) {
        max_length = current_length;
    }

    return max_length; // Возвращаем максимальную длину серии
}
