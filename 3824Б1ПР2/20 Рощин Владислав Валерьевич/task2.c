// Задача 2. Дан непустой массив A, содержащий n натуральных чисел.
// Назовем «серией» группу подряд идущих одинаковых элементов, 
// а длиной серии – количество этих элементов (длина серии может быть равна 2).
// Найти число серий длины 3 в массиве.
#include <iostream>
#include <limits.h>
#include <locale.h>
using namespace std;

int task2(unsigned A[], size_t n)
{
    setlocale(LC_ALL, "rus");

    // Если размер массива меньше 3, серии длины 3 быть не может
    if (n < 3) {
        return 0;
    }

    int seriesCount = 0; // Счетчик серий длины 3

    // Идем по массиву до (n - 2), чтобы проверить тройки подряд идущих элементов
    for (size_t i = 0; i < n - 2; ++i) {
        // Проверяем, равны ли три подряд идущих элемента
        if (A[i] == A[i + 1] && A[i + 1] == A[i + 2]) {
            ++seriesCount; // Если равно, увеличиваем счетчик
        }
    }

    cout << "Количество серий длины 3: " << seriesCount << endl;
    return 0;
}
